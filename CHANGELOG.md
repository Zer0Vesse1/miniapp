# 更新日志

## [0.3.0] — 2026-06-01

### 新增
- **工具模块**（`/tools`）：侧边栏新增 🛠️ 工具入口，首页新增工具卡片
- **计算器重构**（`/tools/calculator`）：复古 LCD 拟物风格 + 五色分区按键
  - 浅色液晶屏（`#F4F9F9`），实体下沉阴影按键，点击压感动效
  - 五色分区：🟢 SHIFT 绿 / 🟠 AC⌫ 橙 / ⚫ 函数深灰 / ⚪ 数字白 / ⚪ 算符中灰
  - 双层丝印：科学模式下每个按键上方显示第二功能小字（如 `7` 键显示 `n!`）
  - **普通模式**：4×5，基础运算 / **高级模式**：5 列科学函数 / **科学模式**：6 列高密度 + SHIFT 二阶状态机
  - 新增阶乘 `n!`、常数 `e`、平方 `x²`、倒数 `x⁻¹`、`EXP` 键
- **颜色转换器**（`/tools/color-converter`）：上下两区独立转换，按钮驱动，红色结果 + 实时色块预览
- **全局历史记录面板**：右上角 📋 按钮，右侧滑出面板
  - 所有工具页面共享：计算器、颜色转换、麻将胡结算的操作结果自动记录
  - 每条记录标注工具名 + 时间戳 + 摘要，localStorage 持久化
  - 面板内可一键清空全部历史
- **字体大小设置**：外观分组新增 小 / 中 / 大，`html` 根元素 `font-size` 缩放，全局 `rem` 自动适配
- **设置页重组**：外观分组（基础颜色 + 字体大小）、关于分组（项目名称 / 技术栈 / 版本号），选择即生效
- **攻略层级改为游戏优先**：列表按游戏分组卡片 → 点击进入子攻略列表 → 再点击看详情
- 新增 **CS2** 攻略入口（空内容待填充）
- 侧边栏首页与其他模块间加分隔线区分

### 移除
- 设置页"应用"按钮：体验不佳，恢复为选择即生效

### 修复
- 字体大小 CSS 选择器从 `body` 改为 `html`，确保全页面缩放生效
- 主题初始化改为 `index.html` 内联脚本 + `setAttribute`，消除模块级代码导致的偶发白屏

### 变更
- 攻略示例从塞尔达换为星露谷物语，slug `sample` → `stardew-beginner`
- 三个游戏页面添加"← 返回游戏列表"链接

## [0.2.0] — 2026-05-31

### 新增
- **主题系统**：浅色 / 深色 / 跟随系统 三种模式
  - `ThemeContext` + `ThemeProvider`（localStorage 持久化 + `matchMedia` 跟踪系统偏好）
  - 新增 CSS 变量：`--color-code-bg`、`--color-on-accent`
  - 修复所有组件硬编码颜色，全面适配浅色主题
- **设置页面**（`/settings`）：侧边栏底部 ⚙️ 设置入口
  - 基础颜色：下拉选择框（浅色 / 深色 / 跟随系统）
  - 关于：显示版本号（读取 `package.json`）

## [0.1.1] — 2026-05-31

### 优化
- **攻略助手加载健壮性**：`useGuide` / `useGuideList` Hook 增加 `error` 状态
  - `fetch` 404 时提示"攻略文件不存在"，网络中断时提示"网络连接失败"
  - 攻略详情页：Loading 阶段展示骨架屏（Skeleton），不再显示纯文字
  - 攻略详情页：Error 阶段展示带图标的错误卡片 + 文件路径提示 + 返回按钮
  - 攻略列表页：Loading 阶段展示骨架卡片动画

## [0.1.0] — 2026-05-31

### 新增
- 项目初始化：React 18 + TypeScript + Vite 脚手架
- 侧边栏导航布局，React Router v6 路由
- 深色主题 CSS 变量体系，响应式适配
- 首页 Dashboard 模块入口卡片

### 新增 · 小游戏
- **2048** — 键盘 + 触屏操作，撤销、最高分 localStorage
- **贪吃蛇** — Canvas 渲染，WASD/方向键操控，速度递增
- **俄罗斯方块** — Canvas 渲染，Ghost 预览、等级加速、下一块预览

### 新增 · 攻略助手
- Markdown 文件驱动：放入 `public/guides/` 自动读取
- Frontmatter 元数据支持（标题、游戏名、标签、日期）
- 攻略列表页：搜索 + 标签筛选
- 攻略详情页：GFM 表格、代码高亮、`::tip::` 提示框、`::timeline::` 时间线
- 示例攻略：塞尔达传说王国之泪新手入门
